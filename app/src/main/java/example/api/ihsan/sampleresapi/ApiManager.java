package example.api.ihsan.sampleresapi;


public class ApiManager {

    /*
    ApiClient api = new ApiClient();
    public final String BASE_URL = "http://akfshjafdssf/";
    public void getTermsList(Request request, ApiCallback<Terms> callback){

        // Todo : execute an httpcall;
       //  api.execute();
        HttpClient client = new HttpClient(BASE_URL);
        client.setParams(request);
        client.setHeader("Accepts", "application/json");
        client.setType(GET);
        HttpResponse response = client.execute();

        if(response.errorCode() == 200){
            callback.onRequestComplete((Terms)response.getBody());
        }else {
            callback.onError(response.errorCode(), response.getErrorMessage());
        }
    }

    */
}
